export class CreditCard {
  id: string;
  name: string;
  number: string;

  constructor(id: string, name: string, number: string) {
    this.id = id;
    this.name = name;
    this.number = number;
  }
}
